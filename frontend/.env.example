# Basis-URL des Backends. Leer lassen fuer den Vite-Dev-Proxy (empfohlen lokal).
# In Produktion (Vercel) bleibt der Wert leer – Frontend und API teilen sich
# dieselbe Domain (https://kniff.app, Rewrites in vercel.json).
VITE_API_BASE=
